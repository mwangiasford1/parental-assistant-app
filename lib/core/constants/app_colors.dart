// core/constants/app_colors.dart
import 'package:flutter/material.dart';

// NOTE: For dark mode support, prefer using Theme.of(context) for colors in widgets.
// These constants are for legacy/light mode only.
class AppColors {
  static const Color primary = Color(0xFF4CAF50); // Calm green
  static const Color accent = Color(0xFFFFC107); // Bright yellow-orange
  static const Color background = Color(0xFFF5F5F5); // Soft neutral
  static const Color card = Color(0xFFE0F2F1); // Light teal
  static const Color textDark = Color(0xFF212121);
  static const Color textLight = Color(0xFFFFFFFF);
}
